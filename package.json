{
  "name": "baseweight-site",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "setup:browsers": "playwright install firefox && sudo apt-get install -y libasound2"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
